// Reporte de auditoría por rango de fechas y sede, exportable (RF-16, RF-17).
export default function Reports() {
  return (
    <section>
      <h2>Reportes de auditoría</h2>
      {/* TODO: filtros fecha desde/hasta + sede, tabla y botón exportar */}
    </section>
  );
}
